# Intentionally empty

Firmware binaries are served from the shared root `firmware/` tree (same origin), not from `new/firmware/`. The new flasher resolves artifact paths against the site root (`/firmware/<board>/...`). Do not add binaries here.
